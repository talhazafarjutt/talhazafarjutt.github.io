# Resume documents — kept local, never commit (contain personal contact info)
*.docx
*.doc
*.pdf

# OS junk
.DS_Store
Thumbs.db
desktop.ini
.Spotlight-V100
.Trashes

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# If npm/node is ever introduced
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Build output (if a build step is ever added)
dist/
build/
.cache/
.parcel-cache/

# Secrets — never commit
.env
.env.*
*.key
*.pem
*.p12
*.pfx
secrets.json
credentials.json
